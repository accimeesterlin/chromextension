import { connect } from 'react-redux';
import ProfileUI from './ProfileUI';


const mapStateToProps = () => {

    return {

    };
};


const mapDispatchToProps = () => {

    return {

    };
};

const Profile = connect(mapStateToProps, mapDispatchToProps)(ProfileUI)

export default Profile;